# Syrian Rates API

Backend يجلب أسعار الصرف الرسمية من مصرف سوريا المركزي.

## Endpoints

- `GET /api/rates` — آخر نشرة (شراء، مبيع، وسطي، تاريخ)
- `GET /api/health` — حالة السيرفر
- `GET /docs` — Swagger UI

## التحديث

كل ساعة تلقائياً من cb.gov.sy
